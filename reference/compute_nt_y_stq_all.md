# Compute all summary scores for nt_y_stq

This function computes all summary scores for the nt_y_stq form. Make
sure to have all necessary columns in the data frame.

## Usage

``` r
compute_nt_y_stq_all(data)
```

## Arguments

- data:

  tbl. Data frame containing the columns to be summarized.

## Value

tbl. The input data frame with the summary scores appended as new
columns.

## Examples

``` r
if (FALSE) { # \dontrun{
compute_nt_y_stq_all(data)
} # }
```
